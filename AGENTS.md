# JustForShow 官网仓库约束

#仓库约束 #官网 #JustForShow

## 提交与推送

- 提交信息只写版本号（如 `v1.0.0`），不写改动说明、开发过程或任何操作细节；改动内容记录在 `docs/site-design.md` 的版本表中，不进提交信息。
- Git 身份固定使用产品身份：`JustForShow <justforshow@weibaba.fun>`（仓库级配置，勿改回个人身份）。
- 分支流程：`dev` 开发 → 合并 `main` 发布 → 建 `release/vX.Y.Z` 分支 + 轻量标签 `vX.Y.Z`（无标签消息）→ 切回 `dev`。

## 站点红线（硬性，违者返工）

- 纯静态、零追踪：不引入统计/分析、外部字体与 CDN 资源、外链图片、表单、Cookie、任何第三方请求；全站无 JS 或仅内联无外部依赖 JS。
- 仓库不得出现用户数据、操作日志、凭据、本机路径等任何非公开信息（含截图内的路径与示例数据）。
- 图标与 logo 全部由应用源图标脚本化派生，禁止各尺寸单独手改。

## 口径管理

- 站点口径以 `docs/site-design.md` 为准；口径变更先回写该文档，再改代码。
- 产品事实（功能、格式、版本、下载渠道、隐私政策）只能来自应用仓库权威文件（`README.md`、`README.en.md`、`PRIVACY.md`、`store/identity.md`），禁止编造或改数。
- 隐私政策页内容 = 应用仓库 `PRIVACY.md`，不增删事实。
